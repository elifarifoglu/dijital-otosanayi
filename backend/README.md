# Backend

Bu klasör FastAPI tabanlı backend uygulamasını içerir.

## Planlanan yapı
- `app/`: uygulama kodları
- `tests/`: testler
- `.env.example`: örnek environment değişkenleri
- `requirements.txt`: Python bağımlılıkları